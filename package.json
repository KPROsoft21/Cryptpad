{
  "name": "cryptpad-ts",
  "version": "1.0.0",
  "description": "CryptPad — TypeScript + Electron edition",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^35.0.0",
    "typescript": "^5.8.0"
  }
}
